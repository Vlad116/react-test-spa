export interface User {
    id: number
    fullName: string
    email: string
    phone: string
    avatar: string
    organization: string
    sector: string
    isCounterparty: boolean
}

export interface Project {
    id: number
}

export interface Structure {
    id: number
}
